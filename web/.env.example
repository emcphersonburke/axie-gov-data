# Where the SPA fetches the precomputed snapshot. Default is same-origin /data/dashboard.json.
# VITE_DATA_URL=/data/dashboard.json
# For local dev against production data, proxy /data instead of setting VITE_DATA_URL (avoids CORS):
# DEV_DATA_PROXY=https://<your-domain>
